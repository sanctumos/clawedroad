# =============================================================================
# MVP Marketplace — Environment Template
# Copy to .env in this folder (app/.env). .env is not committed (see .gitignore).
# PHP loads ONLY the vars listed under "PHP (web)" below (08.9).
# Python-only vars (mnemonic, Alchemy, commission wallets) must NEVER be
# loaded by PHP — they are used only by the Python cron.
# =============================================================================

# -----------------------------------------------------------------------------
# PHP (web) — loaded by PHP only
# -----------------------------------------------------------------------------
DB_DRIVER=sqlite
# For SQLite: db/ folder at same level as public/ (path relative to baseDir = app/)
DB_DSN=sqlite:db/store.sqlite
# For MariaDB: DSN + credentials
# DB_DRIVER=mariadb
# DB_DSN=mysql:host=127.0.0.1;dbname=store;charset=utf8mb4
# DB_USER=store
# DB_PASSWORD=secret

SITE_URL=https://example.com
SITE_NAME=Marketplace
SESSION_SALT=change-me-random-session-salt
COOKIE_ENCRYPTION_SALT=change-me-random-cookie-salt
CSRF_SALT=change-me-random-csrf-salt

# -----------------------------------------------------------------------------
# Python only — NEVER load these in PHP (cron uses them)
# -----------------------------------------------------------------------------
# MNEMONIC=twelve word mnemonic phrase for HD escrow derivation
# ALCHEMY_API_KEY=your-alchemy-api-key
# ALCHEMY_NETWORK=mainnet
# COMMISSION_WALLET_MAINNET=0x...
# COMMISSION_WALLET_SEPOLIA=0x...
# COMMISSION_WALLET_BASE=0x...
